# =============================================================================
# LEASAP BACKEND - ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# Copy this file to .env and fill in your actual values
# Command: cp env.example .env
# 
# IMPORTANT: Never commit .env file to version control!

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Supabase project URL (found in Supabase Dashboard > Settings > API)
SUPABASE_URL=https://your-project.supabase.co

# Supabase Service Role Key (found in Supabase Dashboard > Settings > API)
# WARNING: Keep this secret! Never commit to version control.
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# Supabase JWT Secret (found in Supabase Dashboard > Settings > API)
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# Supabase Service Role Key (alternative name, same as SUPABASE_SERVICE_KEY)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# PostgreSQL Database URL (found in Supabase Dashboard > Settings > Database)
# Format: postgresql://postgres:[PASSWORD]@[HOST]:[PORT]/postgres
DATABASE_1_URL=postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres

# Secondary Database URL (optional, for syncing features)
# Used for secondary database sync operations
DATABASE_2_URL=postgresql://postgres:your-password@db.your-secondary-project.supabase.co:5432/postgres

# =============================================================================
# VERTEX AI CONFIGURATION (Recommended for Production)
# =============================================================================
# Enable Vertex AI (set to "true" to use Vertex AI, "false" to use Gemini API)
USE_VERTEX_AI=true

# Google Cloud Project ID (found in GCP Console)
GCP_PROJECT_ID=your-gcp-project-id

# GCP Region for Vertex AI (us-central1, us-east1, europe-west1, etc.)
GCP_LOCATION=us-central1

# Vertex AI Text Generation Model
# Options: gemini-2.0-flash-exp, gemini-1.5-pro, gemini-1.5-flash
VERTEX_AI_MODEL=gemini-2.0-flash-exp

# Vertex AI Embedding Model
# Options: textembedding-gecko@003, textembedding-gecko@002
VERTEX_AI_EMBEDDING_MODEL=textembedding-gecko@003

# =============================================================================
# GEMINI API CONFIGURATION (Fallback)
# =============================================================================
# Gemini API Key (used as fallback if Vertex AI is not configured)
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# =============================================================================
# AI MODEL CONFIGURATION (Optional Overrides)
# =============================================================================
# Embedding model name (fallback if not using Vertex AI)
EMBEDDING_MODEL_NAME=BAAI/bge-large-en-v1.5

# LLM model name (fallback if not using Vertex AI)
LLM_MODEL_NAME=models/gemini-2.0-flash

# Chunk size for document processing
CHUNK_SIZE=800

# Chunk overlap for document processing
CHUNK_OVERLAP=50

# =============================================================================
# VAPI CONFIGURATION
# =============================================================================
# VAPI API Key (for voice AI assistant)
VAPI_API_KEY=your-vapi-api-key

# VAPI Assistant ID
VAPI_ASSISTANT_ID=your-vapi-assistant-id

# VAPI Secondary Configuration (optional)
VAPI_API_KEY2=your-vapi-api-key-2
VAPI_ASSISTANT_ID2=your-vapi-assistant-id-2

# =============================================================================
# TWILIO CONFIGURATION
# =============================================================================
# Twilio Account SID (found in Twilio Console)
TWILIO_ACCOUNT_SID=your-twilio-account-sid

# Twilio Auth Token (found in Twilio Console)
TWILIO_AUTH_TOKEN=your-twilio-auth-token

# Twilio Secondary Configuration (optional)
TWILIO_ACCOUNT_SID2=your-twilio-account-sid-2
TWILIO_AUTH_TOKEN2=your-twilio-auth-token-2

# =============================================================================
# GOOGLE CLOUD AUTHENTICATION
# =============================================================================
# Path to Google Cloud service account key file (for Vertex AI and Calendar API)
# Set this if using service account authentication
# Example: GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-service-account-key.json
# GOOGLE_APPLICATION_CREDENTIALS=

# Google API Key (optional, for Google Calendar API - OAuth is preferred)
# Note: The app uses OAuth flow for Calendar, but this key might be used for other Google services
GOOGLE_API_KEY=your-google-api-key

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# OAuth2 Redirect URI (for Google Calendar integration)
REDIRECT_URI=https://your-backend-url.com/oauth2callback

# Daily message limit
DAILY_LIMIT=50

# Default timezone
DEFAULT_TIMEZONE=Asia/Karachi

# Working hours (24-hour format)
WORKING_HOURS_START=8
WORKING_HOURS_END=21

# Slot duration in minutes
SLOT_DURATION=30

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp env.example .env
# 2. Fill in all required values above
# 3. For Vertex AI setup, see VERTEX_AI_SETUP.md
# 4. Required minimum variables:
#    - SUPABASE_URL, SUPABASE_SERVICE_KEY, DATABASE_1_URL
#    - Either GCP_PROJECT_ID (for Vertex AI) OR GEMINI_API_KEY (for Gemini API)
#    - VAPI_API_KEY, VAPI_ASSISTANT_ID
#    - TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN

